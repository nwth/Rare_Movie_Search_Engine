# 稀有电影搜索网站开发计划

我要设计一个稀有电影的搜索网站，基于现有的搜索引擎（google、yandex、百度以及其他可以搜索到电影资源的网站），用户给出剧照或电影名称，直接返给用户可直接下载的链接；请基于这个描述给出对应开发plan.md文件，你可以先理解下这个需要，再和我确认

这是一个很有挑战性但也非常有价值的构思。针对“稀有电影”这个痛点，目前市面上的通用搜索引擎往往被商业广告或无效信息淹没，而你想要做的是一个高度垂直的聚合搜索与深度解析工具。

为了确保我理解得足够准确，我将你的需求拆解为以下几个核心技术维度。在为你编写 plan.md 之前，请先确认这些点是否符合你的设想：

1. 核心功能拆解
多模态输入：支持“电影名称”（文本）和“剧照/截图”（图像识别）。

多源引擎聚合：不仅是 Google/Yandex，还需涵盖网盘搜索引擎（如阿里云盘、夸克）、垂直电影论坛（如 PT 站、M-Team 相关索引）、以及特定的影视资源聚合站。

自动化解析（最难点）：不仅仅是给出搜索结果页面，而是通过爬虫或自动化脚本，穿透目标网页的广告和重定向，直接提取出资源链接（如 Magnet、ED2K、网盘链接等）。

2. 技术难点预判
逆向图片搜索：电影剧照的识别需要依赖 Yandex 或 Google Lens 的 API，并从中提取出电影的元数据（标题、年代）。

动态爬虫与反爬：大部分资源网站有复杂的验证码和反爬机制，需要用无头浏览器（Playwright/Selenium）或验证码识别服务。

链接有效性校验：稀有资源往往容易失效，后端需要一个异步任务流来检测提取出的链接是否还能下载。

3. 用户体验流程 (Workflow)
用户上传/输入 -> 2. 元数据提取 (识别电影名) -> 3. 并发搜索 (向 10+ 引擎发请求) -> 4. 结果清洗与去重 -> 5. 直接呈现下载地址。

我需要和你确认以下几点：
资源偏好：你更倾向于提供网盘资源（易于转存）、磁力/种子（适合稀有电影），还是在线观看地址？

技术栈倾向：你希望这个项目是基于 Python（爬虫生态丰富）还是 Node.js（高并发处理强）？

合法性边界：这个网站是作为纯粹的工具（不存储资源，仅聚合）还是带有社区属性？

如果你觉得上述理解没问题，或者有需要补充的地方，请告诉我。确认后我将立刻为你输出详细的 plan.md。

1，资源偏好磁力种子，网盘资源其次，在线观看地址兜底；2，目前基于python，但后续保留架构升级可能，3，当前先作为工具，需要存储一定的资源，后续可能向社区发展

非常清晰。你的需求是一个“深度聚合+智能识别+直接转化”的资源搜索工具。将“磁力/种子”作为首选是非常专业的做法，因为对于稀有电影，高码率的原始文件（BDRip/Remux）通常存在于 P2P 网络中。

以下是为你制定的 plan.md 开发规划文件。

Rare Movie Search Engine (CineSeeker) 开发计划书
1. 项目定位
一个垂直于稀有电影资源的聚合搜索引擎。通过图像识别和多源爬虫技术，解决用户“看到剧照不知道电影名”以及“搜到电影名找不到下载链接”的痛点。

2. 核心架构 (Architecture)
采用模块化分层设计，确保初期使用 Python 快速交付，后期可将核心模块（如爬虫、识别）微服务化。

接入层 (Frontend): React/Next.js (Web 端) 或简单的 Streamlit (原型阶段)。

逻辑层 (BFF/API): FastAPI (Python)，负责异步任务调度。

引擎层 (Search Engines):

剧照识别引擎：对接 Yandex/Google/Bing 逆向搜图 API。

文本聚合引擎：封装对 Google/Baidu/DuckDuckGo 的搜索指令（Dorking）。

垂直站点引擎：针对 PT 站、磁力索引站、网盘聚合站的定制化爬虫。

提取层 (Link Extractor): 自动解析目标网页，提取 Magnet/Torrent/CloudDrive 链接。

持久层 (Database): PostgreSQL (存储电影元数据与资源链接) + Redis (缓存搜索结果)。

3. 开发阶段规划
第一阶段：原型构建 (Phase 1: Foundation) - 第 1-2 周
目标：实现“文本搜资源”的基础全流程。

核心开发：

搭建 FastAPI 基础框架。

开发“Google Dorking”模块：通过 site: 指令精准锁定常见的磁力站和资源论坛。

编写基础爬虫：适配 3-5 个主流磁力站（如 The Pirate Bay, 1337x）和网盘索引站。

实现链接清洗：从 HTML 中提取标准 Magnet 协议和网盘分享链接。

交付物：一个简单的 API，输入电影名，返回初步清洗后的磁力链接列表。

第二阶段：图像识别增强 (Phase 2: Visual Search) - 第 3-4 周
目标：支持上传剧照直接找资源。

核心开发：

集成图像识别：调用 Yandex Image Search API（目前对电影剧照识别率最高）。

元数据提取：从识别结果中提取电影正式名称、上映年份。

链式搜索逻辑：剧照 -> 电影名 -> 磁力搜索 -> 链接返回。

存储系统：

建立本地缓存表：如果同一个电影被多次搜索，直接从本地 DB 返回资源，减少 API 调用。

第三阶段：深度解析与“兜底”机制 (Phase 3: Deep Crawling) - 第 5-6 周
目标：提升资源覆盖率，实现“磁力 > 网盘 > 在线”的优先级分发。

核心开发：

深度爬虫：使用 Playwright 处理那些需要模拟点击或绕过简单反爬的网页。

优先级排序逻辑：

磁力/种子（优先匹配 1080p/4K 关键字）。

网盘资源（阿里云盘、夸克、百度云）。

在线观看链接（嵌入式播放器地址解析）。

链接状态校验：简单的异步 HEAD 请求，检测链接是否 404（针对网盘）。

第四阶段：优化与扩展准备 (Phase 4: Optimization) - 第 7 周+
目标：提升性能，为社区化做准备。

性能优化：

引入 Celery 异步处理耗时较长的全网搜索任务。

前端接入 WebSocket，实时推送搜索进度。

社区化预留：

设计用户账户系统（收藏夹、求片区）。

开放 API 接口，方便后续与其他影视工具集成。

4. 关键技术细节 (Tech Stack)
模块	推荐技术	备注
语言	Python 3.10+	核心生态丰富
识别	Yandex Reverse Image / SerpApi	剧照识别的首选方案
爬虫	HTTPX (异步) + BeautifulSoup4 / Playwright	处理动态加载页面
数据库	PostgreSQL + Tortoise-ORM	结构化存储资源元数据
搜索策略	Google Dorking (e.g. intitle:index.of? mkv)	绕过搜索引擎过滤，直达资源目录
5. 后续升级预案 (Scalability)
架构升级：若访问量激增，将爬虫模块剥离为分布式爬虫集群（Scrapy-Redis）。

存储升级：引入 Elasticsearch 实现对本地存储资源的全文搜索。

智能识别：自建 CLIP 视觉模型，对剧照进行特征向量化，实现更精准的私有库识别。

下一步行动建议
环境配置：建议先申请一个 SerpApi 的 Key 或者研究 Yandex 搜图的解析脚本。

小规模实验：手动收集 10 个最难找的电影名，测试在 Google 中使用哪些 Dorking 指令能直接搜到磁力链接，将这些逻辑转化为代码。

这份计划是否符合你的预期？如果需要针对某个阶段（比如爬虫策略）进行深度细化，请随时告诉我。

符合，输出plan文件

你的稀有电影搜索网站开发计划书已生成。这份文档详细涵盖了从技术栈选型、核心架构逻辑到分阶段实施的完整流程，特别强化了你提到的“磁力优先”和“Python 基础”要求。

你的开发计划文件已准备就绪
MD icon
 rare_movie...h_dev_plan 
MD

核心开发重点提示：
关于 Dorking 搜索：在第一阶段，建议重点研究如何利用 Google 的高级搜索指令（如 intitle:index.of 或 site:pan.quark.cn），这能极大降低初期爬虫开发的复杂度。

关于以图搜片：由于剧照往往包含电影名信息，Yandex 目前的识别效果优于其他通用引擎，建议作为首选接入点。

关于异步处理：搜索多个引擎是非常耗时的操作，务必在开发初期就考虑使用 asyncio 或 Celery 进行非阻塞处理，以保证用户界面的流畅性。

你可以根据这份计划开始环境搭建，如果需要针对其中某个具体模块（例如具体的爬虫逻辑或 API 接口设计）进行细化，请随时告诉我。





Gemini is AI and can make mistakes.


# Rare Movie Search Engine (CineSeeker) 开发计划书 (Development Plan)

## 1. 项目概述
本项目旨在开发一个高度垂直的稀有电影聚合搜索引擎。通过集成多模态输入（文本与图片）、多源数据抓取以及自动化链接解析，为用户提供“一键式”的稀有电影下载资源。

### 核心设计原则
- **资源优先级**：磁力/种子 (Magnet/Torrent) > 网盘资源 (Cloud Drive) > 在线播放 (Stream)。
- **技术选型**：以 Python 为核心，采用异步架构确保并发搜索性能。
- **演进路线**：初期作为纯净搜索工具，中期引入本地存储，长期发展为电影爱好者社区。

---

## 2. 技术栈 (Tech Stack)

| 维度 | 选型 | 说明 |
| :--- | :--- | :--- |
| **后端框架** | FastAPI (Python 3.10+) | 高性能异步 API，支持并发任务处理 |
| **任务调度** | Celery + Redis | 处理耗时较长的全网搜索与爬虫任务 |
| **图像识别** | Yandex Reverse Image / SerpApi | 针对剧照的高精度逆向搜索 |
| **爬虫系统** | HTTPX (Async) + Playwright | 兼顾请求效率与动态页面解析 |
| **数据库** | PostgreSQL (Relational) | 存储电影元数据、资源指纹及用户数据 |
| **缓存层** | Redis | 缓存热门搜索结果，降低 API 与爬虫负载 |
| **搜索技术** | Google Dorking / XPath / CSS Selectors | 精准提取磁力链接与网盘地址 |

---

## 3. 核心功能设计方案

### 3.1 图像识别流程 (Visual Search Pipeline)
1. **上传与预处理**：用户上传剧照或截图。
2. **逆向检索**：通过 API 将图片发送至 Yandex 或 Google Lens。
3. **特征解析**：从返回的网页标题、描述中提取关键词（如：*Interstellar 2014 4K*）。
4. **元数据确认**：利用关键词二次查询电影数据库（如 TMDb/豆瓣）获取标准电影名称。

### 3.2 资源聚合与分发逻辑 (Ranking Algorithm)
1. **多源搜索**：并发向磁力站、PT 站索引、主流网盘搜索引擎发起请求。
2. **深度解析**：
   - **磁力**：解析 `magnet:?xt=urn:btih:`。
   - **网盘**：提取分享链接并尝试获取提取码（如夸克、阿里、百度）。
3. **清洗与排序**：
   - 过滤包含“广告”、“病毒”、“虚假视频”关键字的结果。
   - 根据分辨率（2160p > 1080p）、文件大小、做种人数进行加权排序。

---

## 4. 开发阶段规划 (Phased Roadmap)

### 第一阶段：MVP (最小可行性产品) - 第 1-2 周
- [ ] 搭建 FastAPI 后端基础架构。
- [ ] 实现 Google Dorking 模块，针对特定资源后缀（.torrent, .mkv, .mp4）进行精准搜索。
- [ ] 适配 5 个主流磁力站的静态爬虫（BeautifulSoup4）。
- [ ] 完成基础的前端搜索页面（Next.js 或简单的 Streamlit 原型）。

### 第二阶段：视觉引擎与持久化 - 第 3-4 周
- [ ] 集成图片搜索 API，实现“以图搜片”。
- [ ] 接入 PostgreSQL，开始建立本地“资源指纹库”，实现搜索结果的持久化存储。
- [ ] 开发简单的异步检查机制，剔除已失效的网盘链接。

### 第三阶段：动态爬虫与优化 - 第 5-6 周
- [ ] 引入 Playwright 处理需要 JS 渲染或存在基础反爬（如 Cloudflare 等待页）的网站。
- [ ] 引入 Celery 异步任务流，支持“搜索中...”的实时进度推送。
- [ ] 增加在线观看地址作为备选路径。

### 第四阶段：扩展与社区化预演 - 第 7 周+
- [ ] 设计用户账户体系，支持资源收藏。
- [ ] 完善 API 接口文档，为后续移动端或插件开发打下基础。
- [ ] 增加“求片”功能，作为社区互助的基础模块。

---

## 5. 存储策略 (Storage Strategy)
1. **元数据层**：存储电影的名称、年代、海报、导演、IMDb ID。
2. **资源关联层**：存储电影 ID 与各平台资源链接的映射。
3. **指纹校验**：通过磁力链接的 InfoHash 避免重复存储。

---

## 6. 未来升级建议
- **容器化部署**：使用 Docker + Docker Compose 快速部署整个环境。
- **分布式爬虫**：若搜索量增加，可考虑 Scrapy-Redis 集群。
- **本地 AI 模型**：在后期可考虑训练本地的剧照分类模型，减少对昂贵第三方 API 的依赖。
# Rare Movie Search Engine (CineSeeker) 开发计划书 (Development Plan)

## 1. 项目概述
本项目旨在开发一个高度垂直的稀有电影聚合搜索引擎。通过集成多模态输入（文本与图片）、多源数据抓取以及自动化链接解析，为用户提供“一键式”的稀有电影下载资源。

### 核心设计原则
- **资源优先级**：磁力/种子 (Magnet/Torrent) > 网盘资源 (Cloud Drive) > 在线播放 (Stream)。
- **技术选型**：以 Python 为核心，采用异步架构确保并发搜索性能。
- **演进路线**：初期作为纯净搜索工具，中期引入本地存储，长期发展为电影爱好者社区。

---

## 2. 技术栈 (Tech Stack)

| 维度 | 选型 | 说明 |
| :--- | :--- | :--- |
| **后端框架** | FastAPI (Python 3.10+) | 高性能异步 API，支持并发任务处理 |
| **任务调度** | Celery + Redis | 处理耗时较长的全网搜索与爬虫任务 |
| **图像识别** | Yandex Reverse Image / SerpApi | 针对剧照的高精度逆向搜索 |
| **爬虫系统** | HTTPX (Async) + Playwright | 兼顾请求效率与动态页面解析 |
| **数据库** | PostgreSQL (Relational) | 存储电影元数据、资源指纹及用户数据 |
| **缓存层** | Redis | 缓存热门搜索结果，降低 API 与爬虫负载 |
| **搜索技术** | Google Dorking / XPath / CSS Selectors | 精准提取磁力链接与网盘地址 |

---

## 3. 核心功能设计方案

### 3.1 图像识别流程 (Visual Search Pipeline)
1. **上传与预处理**：用户上传剧照或截图。
2. **逆向检索**：通过 API 将图片发送至 Yandex 或 Google Lens。
3. **特征解析**：从返回的网页标题、描述中提取关键词（如：*Interstellar 2014 4K*）。
4. **元数据确认**：利用关键词二次查询电影数据库（如 TMDb/豆瓣）获取标准电影名称。

### 3.2 资源聚合与分发逻辑 (Ranking Algorithm)
1. **多源搜索**：并发向磁力站、PT 站索引、主流网盘搜索引擎发起请求。
2. **深度解析**：
   - **磁力**：解析 `magnet:?xt=urn:btih:`。
   - **网盘**：提取分享链接并尝试获取提取码（如夸克、阿里、百度）。
3. **清洗与排序**：
   - 过滤包含“广告”、“病毒”、“虚假视频”关键字的结果。
   - 根据分辨率（2160p > 1080p）、文件大小、做种人数进行加权排序。

---

## 4. 开发阶段规划 (Phased Roadmap)

### 第一阶段：MVP (最小可行性产品) - 第 1-2 周
- [ ] 搭建 FastAPI 后端基础架构。
- [ ] 实现 Google Dorking 模块，针对特定资源后缀（.torrent, .mkv, .mp4）进行精准搜索。
- [ ] 适配 5 个主流磁力站的静态爬虫（BeautifulSoup4）。
- [ ] 完成基础的前端搜索页面（Next.js 或简单的 Streamlit 原型）。

### 第二阶段：视觉引擎与持久化 - 第 3-4 周
- [ ] 集成图片搜索 API，实现“以图搜片”。
- [ ] 接入 PostgreSQL，开始建立本地“资源指纹库”，实现搜索结果的持久化存储。
- [ ] 开发简单的异步检查机制，剔除已失效的网盘链接。

### 第三阶段：动态爬虫与优化 - 第 5-6 周
- [ ] 引入 Playwright 处理需要 JS 渲染或存在基础反爬（如 Cloudflare 等待页）的网站。
- [ ] 引入 Celery 异步任务流，支持“搜索中...”的实时进度推送。
- [ ] 增加在线观看地址作为备选路径。

### 第四阶段：扩展与社区化预演 - 第 7 周+
- [ ] 设计用户账户体系，支持资源收藏。
- [ ] 完善 API 接口文档，为后续移动端或插件开发打下基础。
- [ ] 增加“求片”功能，作为社区互助的基础模块。

---

## 5. 存储策略 (Storage Strategy)
1. **元数据层**：存储电影的名称、年代、海报、导演、IMDb ID。
2. **资源关联层**：存储电影 ID 与各平台资源链接的映射。
3. **指纹校验**：通过磁力链接的 InfoHash 避免重复存储。

---

## 6. 未来升级建议
- **容器化部署**：使用 Docker + Docker Compose 快速部署整个环境。
- **分布式爬虫**：若搜索量增加，可考虑 Scrapy-Redis 集群。
- **本地 AI 模型**：在后期可考虑训练本地的剧照分类模型，减少对昂贵第三方 API 的依赖。
